import { FormatDateForListPipe } from './format-date-for-list.pipe';

describe('FormatDateForListPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateForListPipe();
    expect(pipe).toBeTruthy();
  });
});
