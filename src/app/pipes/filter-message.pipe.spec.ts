import { FilterMessagePipe } from './filter-message.pipe';

describe('FilterMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
